"use client";
import Link from "next/link";
import { useState } from "react";
import { createClient, isSupabaseConfigured } from "@/lib/supabase/client";
export function ResetForm(){const [loading,setLoading]=useState(false);const [message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);try{const email=String(new FormData(e.currentTarget).get("email"));await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/auth/callback?flow=recovery`});setMessage("Se existir uma conta com este e-mail, enviaremos um link para redefinir sua senha. Verifique também o spam.")}catch{setMessage("Se existir uma conta com este e-mail, enviaremos um link para redefinir sua senha. Tente novamente em alguns minutos.")}finally{setLoading(false)}}return <div className="auth-card af-card"><h2>Recuperar <span>senha</span></h2><p>Informe seu e-mail para receber um link seguro.</p><form onSubmit={submit}><label>E-mail<div className="input-wrap"><input required type="email" name="email" autoComplete="email" placeholder="seu@email.com"/></div></label>{message&&<div role="status" className="auth-notice">{message}</div>}{!isSupabaseConfigured()&&<div className="auth-notice">Configure o Supabase para enviar o e-mail.</div>}<button className="af-btn af-btn-primary submit" disabled={loading}>{loading?"Enviando...":"Enviar link de recuperação"}</button></form><div className="signup-copy"><Link href="/login">Voltar para o login</Link></div></div>}
